<%= link_to("新規投稿", "/users/new") %>
<% @users.each do |user| %>
  <p>
    <%= user.name %>
    <%= user.age %>
    <%= link_to("編集", "/users/#{user.id}/edit")%>
    <%= link_to("削除", "/users/#{user.id}/destroy", {method: :delete}) %>
  </p>
<% end %>
