<%= form_for @user, url:"/users/#{@user.id}/update", html:{method: :patch} do |f| %>
  <h2>編集</h2>
  <%= f.label :name %>
  <%= f.text_field :name%>
  <%= f.label :age%>
  <%= f.text_field :age%>
  <input type="submit" value="保存">
<% end %>
